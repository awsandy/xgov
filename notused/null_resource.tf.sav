resource "null_resource" "lf-setup" {
  triggers = {
    always_run = timestamp()
  }
  depends_on=[aws_cloudformation_stack.lf-users]
  provisioner "local-exec" {
    when    = create
    command = "./01-lf-setup.sh"
  }
}